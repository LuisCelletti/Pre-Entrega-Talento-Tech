export const actualizacrContador = (carrito) => {
    const contador = document.getElementById("contador-Carrito");
    if(contador){
        contador.textContent = carrito.length
    }   
    } 

    export|const mostrarMensaje = (mensaje,) => {
        alert(texto)
        } 